{
  "builds": [
    {
      "src": "src/index.ts",  // Adjust based on your entry file
      "use": "@vercel/node"
    }
  ],
  "outputDirectory": "dist"  // Set this to your build output directory
}
